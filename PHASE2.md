# 햇반이네 Phase 2 결과

## 수정 1 — 지뢰찾기 도움말 복원

[문제]  
지뢰찾기 게임의 팁 버튼이 존재하지 않는 `mine-help-modal`을 조회했다.

[원인]  
`openMineHelp`와 `closeMineHelp` 함수 및 팁 버튼은 남아 있었지만 대응하는 HTML 모달만 누락돼 있었다.

[수정]  
기존 노란색 팁 버튼과 둥근 카드 디자인에 맞는 작은 도움말 모달을 복원했다. 기존 두 전역 함수를 그대로 사용한다.

[쉽게 말하면]  
지뢰찾기에서 팁을 누르면 조작법이 열리고 닫기와 재열기가 정상 동작한다.

[검증]  
브라우저에서 지뢰찾기 진입 → 게임 시작 → 팁 열기 → 닫기 → 다시 열기를 실행했다. 두 번 모두 표시됐고 앱 오류가 없었다.

[리스크]  
새 모달의 안내 문구가 원래 누락되기 전 문구와 완전히 같았는지는 확인할 원본이 없다. 기존 기능 위치와 스타일은 유지했다.

## 수정 2 — 메모 색상의 전역 event 의존 제거

[문제]  
`selectMemoColor`가 인자로 받지 않은 전역 `event.target`을 사용했다.

[원인]  
색상 버튼 클릭뿐 아니라 저장된 메모를 여는 코드에서도 함수를 직접 호출하는데, 이때 신뢰할 수 있는 click event가 없다.

[수정]  
색상 버튼의 inline handler가 `this`를 두 번째 인자로 넘기도록 했다. 직접 호출될 때는 색상 class와 일치하는 버튼을 찾는다.

[쉽게 말하면]  
새 메모와 기존 메모 모두 어떤 브라우저에서든 선택한 색상이 정확히 표시된다.

[검증]  
네 가지 색상 선택 표시, 메모 저장, 새로고침, 기존 메모 열기, 초록색으로 변경, 다시 새로고침을 브라우저에서 확인했다.

[리스크]  
기존 `window.selectMemoColor` 이름과 첫 번째 인자는 유지돼 기존 호출부와 호환된다.

## 수정 3 — localStorage JSON 안전 파싱

[문제]  
JSON 저장값 하나가 손상되면 `JSON.parse` 예외가 onload를 중단할 수 있었다.

[원인]  
각 저장 키가 별도 방어 없이 직접 파싱됐다.

[수정]  
`safeParseJSON(value, fallback, storageKey)`를 추가하고 다음 7개 JSON 키에 적용했다.

- `hatban_profile`
- `hatban_dday`
- `hatban_game_records`
- `hatban_quote`
- `hatban_ratings`
- `hatban_memos`
- `hatban_notebooks`

파싱 실패 시 warning을 남기고 메모리의 기본값으로 계속 실행한다. 손상된 저장값을 삭제하거나 fallback으로 저장하지 않는다.

[쉽게 말하면]  
저장 데이터 하나가 깨져도 앱 전체가 흰 화면이나 초기화 중단 상태가 되지 않는다.

[검증]  
추출한 실제 helper에 잘못된 JSON과 정상 JSON을 입력하는 단위 검사를 통과했다. `removeItem` 호출이 없고 storage key 목록도 기존과 같음을 확인했다.

[리스크]  
JSON 문법은 정상이지만 내부 필드 구조가 심하게 변조된 경우까지 완전 복구하지는 않는다. 이번 단계에서는 기존 데이터 구조를 추측해 재작성하지 않았다.

## 수정 4 — interval 책임 분리

### 변경 전

| 기능 | interval 생성 | 저장 위치 | 종료 방식 | `clearAllIntervals` 영향 |
|---|---|---|---|---|
| 시간표 | 직접 `setInterval` | 저장 안 함 | 페이지 종료 | 영향 없음 |
| 타이머 | `safeSetInterval` | `tInterval`과 공통 배열 | 개별 또는 공통 종료 | 게임에 의해 중단 가능 |
| 스톱워치 | `safeSetInterval` | `swInterval`과 공통 배열 | 개별 또는 공통 종료 | 게임에 의해 중단 가능 |
| 짝맞추기 | `safeSetInterval` | 공통 배열 | 공통 종료 | 모든 시간 기능 중단 |
| 암산 | `safeSetInterval` | 공통 배열 | 공통 종료 | 모든 시간 기능 중단 |
| 10초 게임 | `safeSetInterval` | 공통 배열 | 공통 종료 | 모든 시간 기능 중단 |

### 변경 후

| 범주 | 관리 방식 | 서로에게 미치는 영향 |
|---|---|---|
| 시간표 | 독립적인 직접 interval | 다른 기능과 무관 |
| 타이머 | `tInterval` | 게임 정리의 영향을 받지 않음 |
| 스톱워치 | `swInterval` | 게임 정리의 영향을 받지 않음 |
| 게임 | `gameIntervals.match/math/tensec` | 게임별로 하나씩 시작/정리 |
| 게임 메뉴 복귀 | `clearAllGameIntervals` | 게임 interval만 전체 정리 |

[쉽게 말하면]  
게임을 시작하거나 나와도 타이머와 스톱워치는 자기 상태대로 계속 동작한다. 게임들은 자기 interval만 교체한다.

[검증]  
helper 단위 검사에서 match만 종료했을 때 math가 남고, 전체 게임 종료에서 두 슬롯이 모두 정리되는 것을 확인했다. 브라우저에서는 타이머를 실행한 채 시간제한 짝맞추기를 진행해 타이머가 계속 감소하는 것을 확인했다.

[리스크]  
게임 종료 알림 등에 사용되는 짧은 `setTimeout`은 이번 interval 범위에 포함하지 않았다.

## 수정 5 — 짝맞추기 설정 복귀 시 timer 정리

[문제]  
시간제한 짝맞추기 중 설정으로 돌아가도 백그라운드 제한시간이 계속 진행됐다.

[원인]  
`showMatchConfig`에 match interval 종료가 없었다.

[수정]  
함수 시작 시 `clearGameInterval('match')`를 호출한다.

[쉽게 말하면]  
설정 버튼을 누르는 즉시 그 판의 시계가 멈추며, 다시 시작하면 새 시계 하나만 동작한다.

[검증]  
브라우저에서 시간 모드가 58초일 때 설정으로 복귀하고 1.6초를 기다렸다. 표시값이 계속 58초였고 동시에 실행한 일반 타이머는 계속 진행됐다.

[리스크]  
다른 게임, 타이머, 스톱워치 interval은 종료하지 않는다.

## 수정 6 — 사용자 입력 HTML 삽입

### 분류

| 분류 | 항목 | 처리 |
|---|---|---|
| A — 안전 | 프로필 제목/이모지, 오늘의 문구, D-Day 이름, 계산기 표시 | 기존 `innerText`/value 유지 |
| B — 내부 생성값 | 시간표 과목/시간, 게임 기록 라벨, 모아보기 탭 제목, Canvas data URL | 이번 단계 유지 |
| C — 수정 필요 | 메모 본문, 배움공책 본문, 숫자야구 입력 로그 | 안전한 텍스트 렌더링으로 변경 |

[수정]  
메모와 숫자야구 로그는 DOM 요소의 `textContent`로 넣는다. 배움공책은 텍스트를 escape한 뒤 기존처럼 줄바꿈만 `<br>`로 표시한다. 메모 삭제 inline handler는 유지하고 사용자 ID는 `dataset`으로 전달한다.

[쉽게 말하면]  
사용자가 `<b>` 같은 문자를 입력해도 HTML로 실행되지 않고 입력한 글자 그대로 보인다. 줄바꿈과 카드 디자인은 유지된다.

[검증]  
브라우저 메모에서 `Phase2 <b>안전</b>`과 줄바꿈을 저장·새로고침해 literal text와 escaped DOM을 확인했다. 배움공책에서도 같은 형태의 문구와 둘째 줄이 그대로 표시되는 것을 확인했다.

[리스크]  
조작된 localStorage의 Canvas URL이나 앱 내부 키까지 완전히 신뢰하지 않는 별도 sanitization은 이번 범위가 아니다.

## localStorage compatibility

- 기존 9개 `hatban_*` 키 이름은 모두 동일하다.
- 저장 시 사용하는 JSON 객체/배열 구조는 변경하지 않았다.
- 구형 게임 기록 compatibility 분기를 유지했다.
- 문자열형 구형 notebook 데이터와 객체형 notebook 데이터 처리를 유지했다.
- 파싱 실패 시 해당 저장값을 삭제하거나 덮어쓰지 않는다.

## 브라우저 검증

Node 내장 HTTP 서버로 `http://127.0.0.1:8123/`에서 실행했다. 프로젝트 의존성은 추가하지 않았다.

### 실행한 테스트

- 초기 앱 로딩
- 지뢰찾기 시작, 팁 열기/닫기/재열기
- 메모 네 색상 선택 UI
- HTML 특수문자와 줄바꿈 메모 저장
- 메모 새로고침 유지와 색상 수정/재유지
- 타이머 실행 중 시간제한 짝맞추기 시작
- 짝맞추기 설정 복귀 후 match timer 정지
- 게임 동작 중 일반 타이머 계속 진행
- 배움공책 HTML 특수문자와 줄바꿈 표시
- Console warning/error 확인

앱 코드 error는 없었다. 기존 Tailwind CDN의 production 권고 warning만 발생했다.

### 브라우저에서 실행하지 못한 테스트

- 손상된 localStorage를 직접 주입하는 테스트: 브라우저 자동화의 page evaluation은 읽기 전용이어서 저장값 변조를 하지 않았다. 실제 helper 단위 검사와 정적 저장 호출 검사로 대체했다.
- 터치 Canvas와 우클릭/길게 누르기 같은 실제 장치 입력
- 전체 게임 완주와 게임 기록 순위 생성

## 남은 이슈

### 높음

- 이번 Phase에서 확인된 높은 심각도 항목은 모두 처리했다.

### 중간

- JSON 문법은 맞지만 예상 필드가 빠진 데이터에 대한 깊은 shape 검증
- 게임을 나간 뒤에도 이미 예약된 일부 짧은 `setTimeout` callback이 실행될 가능성
- 타이머 종료 알람이 설정 시간보다 약 한 tick 늦게 발생하는 로직

### 낮음

- 다운로드 실패 시 화면 밖 clone DOM 정리
- 하루 삭제 undo timeout 경합
- 계산기의 `new Function` 사용
- 버전이 고정되지 않은 Tailwind/SortableJS CDN
