import { gameList, mountGame } from './gameShell.js';
export function renderGamesView(){const root=document.createElement('section');root.className='screen';let cleanup=()=>{};const heading=document.createElement('div');heading.className='page-heading';heading.innerHTML='<div><span class="eyebrow">짧게 놀고 다시 집중!</span><h1>집중 게임 🎮</h1><p>원하는 게임을 골라 시작해요.</p></div>';function menu(){cleanup();root.replaceChildren(heading,gameList(open));window.scrollTo({top:0,behavior:'instant'});}function open(id){const game=mountGame(id,menu);cleanup=game.destroy;root.replaceChildren(game.element);window.scrollTo({top:0,behavior:'instant'});}menu();return {element:root,destroy(){cleanup();}};}
